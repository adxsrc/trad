# `Trad`

`Trad` is a GPU-accelerated radiative transfer package.
`Trad` is written in python and uses
[Google's JAX](https://github.com/google/jax)
package for performance.
While not as fast as C codes such as
[`GRay`](https://github.com/luxsrc/gray),
`Trad` is easily extensible.
